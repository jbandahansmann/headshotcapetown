# Phase 2 — dedicated landing pages

These pages are referenced from the footer but not yet built. Create them in `app/[slug]/page.tsx` post-launch when you have traffic data. Each should:

- Have its own keyword-targeted `<title>` and meta description
- Reuse the existing components but reorder/reframe for the specific service
- Include a unique H1 with the target keyword
- Cross-link to the homepage and journal articles
- Use the same `LocalBusiness` schema from layout

Suggested pages:
- `/corporate-headshots-cape-town/page.tsx` — corporate team focus
- `/team-headshots-cape-town/page.tsx` — team logistics, on-site setup
- `/linkedin-photographer-cape-town/page.tsx` — individual professionals, LinkedIn-focused copy
